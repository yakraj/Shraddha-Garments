{
  "name": "ujiyala-foundation-root",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "vercel-build": "vite build"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "html2pdf.js": "^0.12.1"
  }
}
